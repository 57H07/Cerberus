# Copy to .env and replace every value. .env is ignored by git.
# SQL Server requires 8+ characters with upper case, lower case, digits and symbols.
SQL_SA_PASSWORD=ChangeMe-Sql-2026!
DEV_CERT_PASSWORD=change-me-dev-cert
BOOTSTRAP_ADMIN_EMAIL=admin@cerberus.local
BOOTSTRAP_ADMIN_USERNAME=admin
BOOTSTRAP_ADMIN_PASSWORD=change-me-admin-password
DEMO_USER_PASSWORD=change-me-demo-password
DEMO_CLIENT_SECRET=change-me-demo-client-secret
